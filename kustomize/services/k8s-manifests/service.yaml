apiVersion: v1
kind: Service
metadata:
  name: svc
  labels:
    app-type: spring-boot
spec:
  selector:
    app-type: spring-boot
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  